<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Analytics - Oollert Tasks</title>
  <link rel="stylesheet" href="/css/styles.css" />
  <style>
    .analytics-container {
      max-width: 1400px;
      margin: 0 auto;
      padding: 32px;
    }
    
    .stats-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
      gap: 20px;
      margin-bottom: 32px;
    }
    
    .stat-card {
      background: white;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 2px 12px rgba(0,0,0,0.08);
      transition: transform 0.2s;
    }
    
    .stat-card:hover {
      transform: translateY(-4px);
      box-shadow: 0 4px 20px rgba(0,0,0,0.12);
    }
    
    .stat-icon {
      font-size: 36px;
      margin-bottom: 12px;
    }
    
    .stat-label {
      font-size: 13px;
      color: #5e6c84;
      font-weight: 600;
      text-transform: uppercase;
      letter-spacing: 0.5px;
      margin-bottom: 8px;
    }
    
    .stat-value {
      font-size: 36px;
      font-weight: 700;
      color: #172b4d;
    }
    
    .stat-card.total { border-left: 4px solid #667eea; }
    .stat-card.completed { border-left: 4px solid #4caf50; }
    .stat-card.pending { border-left: 4px solid #ffa500; }
    .stat-card.overdue { border-left: 4px solid #e63946; }
    
    .charts-section {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 24px;
      margin-bottom: 32px;
    }
    
    .chart-card {
      background: white;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    }
    
    .chart-title {
      font-size: 18px;
      font-weight: 700;
      color: #172b4d;
      margin-bottom: 20px;
    }
    
    .priority-bar {
      margin-bottom: 16px;
    }
    
    .priority-label {
      display: flex;
      justify-content: space-between;
      margin-bottom: 8px;
      font-size: 14px;
      color: #5e6c84;
      font-weight: 600;
    }
    
    .bar-container {
      height: 32px;
      background: #f4f5f7;
      border-radius: 16px;
      overflow: hidden;
      position: relative;
    }
    
    .bar-fill {
      height: 100%;
      border-radius: 16px;
      transition: width 0.5s ease;
      display: flex;
      align-items: center;
      padding: 0 12px;
      color: white;
      font-weight: 600;
      font-size: 13px;
    }
    
    .bar-urgent { background: linear-gradient(90deg, #e63946, #ff6b6b); }
    .bar-high { background: linear-gradient(90deg, #ff6b6b, #ff8787); }
    .bar-medium { background: linear-gradient(90deg, #ffa500, #ffb733); }
    .bar-low { background: linear-gradient(90deg, #4caf50, #66bb6a); }
    
    .completion-circle {
      width: 200px;
      height: 200px;
      margin: 0 auto;
      position: relative;
    }
    
    .circle-bg {
      fill: none;
      stroke: #f4f5f7;
      stroke-width: 20;
    }
    
    .circle-progress {
      fill: none;
      stroke: url(#gradient);
      stroke-width: 20;
      stroke-linecap: round;
      transform: rotate(-90deg);
      transform-origin: 50% 50%;
      transition: stroke-dashoffset 1s ease;
    }
    
    .circle-text {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 48px;
      font-weight: 700;
      color: #172b4d;
    }
    
    .insights-section {
      background: white;
      padding: 24px;
      border-radius: 12px;
      box-shadow: 0 2px 12px rgba(0,0,0,0.08);
    }
    
    .insight-item {
      padding: 16px;
      background: #f9fafb;
      border-radius: 8px;
      margin-bottom: 12px;
      display: flex;
      align-items: center;
      gap: 16px;
    }
    
    .insight-icon {
      font-size: 32px;
    }
    
    .insight-text {
      flex: 1;
    }
    
    .insight-title {
      font-weight: 600;
      color: #172b4d;
      margin-bottom: 4px;
    }
    
    .insight-desc {
      font-size: 14px;
      color: #5e6c84;
    }
    
    @media (max-width: 1024px) {
      .charts-section {
        grid-template-columns: 1fr;
      }
      
      .stats-grid {
        grid-template-columns: repeat(2, 1fr);
      }
    }
    
    @media (max-width: 640px) {
      .stats-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <header>
    <h1>Oollert Tasks</h1>
    <nav>
      <a href="/home">Home</a>
      <a href="/tasks">Board View</a>
      <a href="/tasks/smart">Smart View</a>
      <a href="/tasks/analytics" style="color: #667eea; font-weight: 700;">Analytics</a>
      <form method="POST" action="/logout" style="display:inline">
        <button type="submit">Logout</button>
      </form>
    </nav>
  </header>
  <svg style="position: absolute; width: 0; height: 0;">
  <defs>
    <linearGradient id="pomodoroGradient" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#667eea;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#764ba2;stop-opacity:1" />
    </linearGradient>
    <linearGradient id="pomodoroGradientActive" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" style="stop-color:#4caf50;stop-opacity:1" />
      <stop offset="100%" style="stop-color:#66bb6a;stop-opacity:1" />
    </linearGradient>
  </defs>
</svg>

  <div class="analytics-container">
    <h2 style="font-size: 28px; margin-bottom: 24px; color: #172b4d;">📊 Your Productivity Dashboard</h2>
    
    <!-- Stats Grid -->
    <div class="stats-grid">
      <div class="stat-card total">
        <div class="stat-icon">📋</div>
        <div class="stat-label">Total Tasks</div>
        <div class="stat-value"><%= stats.total %></div>
      </div>
      
      <div class="stat-card completed">
        <div class="stat-icon">✅</div>
        <div class="stat-label">Completed</div>
        <div class="stat-value"><%= stats.completed %></div>
      </div>
      
      <div class="stat-card pending">
        <div class="stat-icon">⏳</div>
        <div class="stat-label">In Progress</div>
        <div class="stat-value"><%= stats.inProgress %></div>
      </div>
      
      <div class="stat-card overdue">
        <div class="stat-icon">⚠️</div>
        <div class="stat-label">Overdue</div>
        <div class="stat-value"><%= stats.overdue %></div>
      </div>
    </div>

    <!-- Charts Section -->
    <div class="charts-section">
      <!-- Priority Distribution -->
      <div class="chart-card">
        <h3 class="chart-title">Tasks by Priority</h3>
        
        <div class="priority-bar">
          <div class="priority-label">
            <span>🔴 Urgent</span>
            <span><%= stats.byPriority.urgent %> tasks</span>
          </div>
          <div class="bar-container">
            <div class="bar-fill bar-urgent" style="width: <%= stats.total > 0 ? (stats.byPriority.urgent / stats.total * 100) : 0 %>%">
              <%= stats.byPriority.urgent %>
            </div>
          </div>
        </div>
        
        <div class="priority-bar">
          <div class="priority-label">
            <span>🟠 High</span>
            <span><%= stats.byPriority.high %> tasks</span>
          </div>
          <div class="bar-container">
            <div class="bar-fill bar-high" style="width: <%= stats.total > 0 ? (stats.byPriority.high / stats.total * 100) : 0 %>%">
              <%= stats.byPriority.high %>
            </div>
          </div>
        </div>
        
        <div class="priority-bar">
          <div class="priority-label">
            <span>🟡 Medium</span>
            <span><%= stats.byPriority.medium %> tasks</span>
          </div>
          <div class="bar-container">
            <div class="bar-fill bar-medium" style="width: <%= stats.total > 0 ? (stats.byPriority.medium / stats.total * 100) : 0 %>%">
              <%= stats.byPriority.medium %>
            </div>
          </div>
        </div>
        
        <div class="priority-bar">
          <div class="priority-label">
            <span>🟢 Low</span>
            <span><%= stats.byPriority.low %> tasks</span>
          </div>
          <div class="bar-container">
            <div class="bar-fill bar-low" style="width: <%= stats.total > 0 ? (stats.byPriority.low / stats.total * 100) : 0 %>%">
              <%= stats.byPriority.low %>
            </div>
          </div>
        </div>
      </div>

      <!-- Completion Rate -->
      <div class="chart-card">
        <h3 class="chart-title">Completion Rate</h3>
        <div class="completion-circle">
          <svg width="200" height="200">
            <defs>
              <linearGradient id="gradient" x1="0%" y1="0%" x2="100%" y2="100%">
                <stop offset="0%" style="stop-color:#667eea;stop-opacity:1" />
                <stop offset="100%" style="stop-color:#764ba2;stop-opacity:1" />
              </linearGradient>
            </defs>
            <circle class="circle-bg" cx="100" cy="100" r="80" />
            <circle class="circle-progress" cx="100" cy="100" r="80" 
                    stroke-dasharray="<%= 2 * Math.PI * 80 %>" 
                    stroke-dashoffset="<%= 2 * Math.PI * 80 * (1 - stats.completionRate / 100) %>" />
          </svg>
          <div class="circle-text"><%= stats.completionRate %>%</div>
        </div>
        <p style="text-align: center; margin-top: 20px; color: #5e6c84;">
          <%= stats.completed %> of <%= stats.total %> tasks completed
        </p>
      </div>
    </div>

    <!-- Insights Section -->
    <div class="insights-section">
      <h3 class="chart-title">💡 Insights & Recommendations</h3>
      
      <% if (stats.overdue > 0) { %>
        <div class="insight-item" style="border-left: 4px solid #e63946;">
          <div class="insight-icon">⚠️</div>
          <div class="insight-text">
            <div class="insight-title">You have <%= stats.overdue %> overdue task<%= stats.overdue > 1 ? 's' : '' %></div>
            <div class="insight-desc">Consider rescheduling or completing these tasks to stay on track</div>
          </div>
        </div>
      <% } %>
      
      <% if (stats.recentCompletions > 0) { %>
        <div class="insight-item" style="border-left: 4px solid #4caf50;">
          <div class="insight-icon">🎉</div>
          <div class="insight-text">
            <div class="insight-title">Great job! You completed <%= stats.recentCompletions %> task<%= stats.recentCompletions > 1 ? 's' : '' %> this week</div>
            <div class="insight-desc">Keep up the momentum!</div>
          </div>
        </div>
      <% } %>
      
      <% if (stats.byPriority.urgent > 0) { %>
        <div class="insight-item" style="border-left: 4px solid #ff6b6b;">
          <div class="insight-icon">🔥</div>
          <div class="insight-text">
            <div class="insight-title"><%= stats.byPriority.urgent %> urgent task<%= stats.byPriority.urgent > 1 ? 's' : '' %> need<%= stats.byPriority.urgent === 1 ? 's' : '' %> attention</div>
            <div class="insight-desc">Focus on high-priority items to maximize productivity</div>
          </div>
        </div>
      <% } %>
      
      <% if (stats.completionRate >= 80) { %>
        <div class="insight-item" style="border-left: 4px solid #667eea;">
          <div class="insight-icon">🌟</div>
          <div class="insight-text">
            <div class="insight-title">Excellent completion rate of <%= stats.completionRate %>%!</div>
            <div class="insight-desc">You're crushing your goals!</div>
          </div>
        </div>
      <% } else if (stats.completionRate < 50 && stats.total > 5) { %>
        <div class="insight-item" style="border-left: 4px solid #ffa500;">
          <div class="insight-icon">💪</div>
          <div class="insight-text">
            <div class="insight-title">Your completion rate is <%= stats.completionRate %>%</div>
            <div class="insight-desc">Break down larger tasks into smaller chunks to improve productivity</div>
          </div>
        </div>
      <% } %>
      
      <% if (stats.total === 0) { %>
        <div class="insight-item" style="border-left: 4px solid #667eea;">
          <div class="insight-icon">🚀</div>
          <div class="insight-text">
            <div class="insight-title">Ready to get started?</div>
            <div class="insight-desc">Create your first task to begin tracking your productivity</div>
          </div>
        </div>
      <% } %>
    </div>
  </div>

  <script>
    // Animate bars on load
    window.addEventListener('load', () => {
      document.querySelectorAll('.bar-fill').forEach((bar, index) => {
        const width = bar.style.width;
        bar.style.width = '0%';
        setTimeout(() => {
          bar.style.width = width;
        }, 100 * index);
      });
    });
  </script>
  <script src="/js/pomodoro.js"></script>
</body>
</html>
