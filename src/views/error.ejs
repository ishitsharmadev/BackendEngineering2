<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Server Error</title>
  <style>body{font-family:Arial,Helvetica,sans-serif;background:#f7f7f7;color:#222;padding:40px} .card{background:#fff;padding:20px;border-radius:6px;max-width:900px;margin:0 auto;box-shadow:0 4px 16px rgba(0,0,0,0.08)} pre{background:#111;color:#0f0;padding:12px;border-radius:4px;overflow:auto}</style>
</head>
<body>
  <div class="card">
    <h1>Internal Server Error</h1>
    <p>Sorry — something went wrong on the server.</p>
    <% if (showStack && error) { %>
      <h3>Stack trace (development only)</h3>
      <pre><%= error.stack %></pre>
    <% } %>
    <p><a href="/">Return home</a></p>
  </div>
</body>
</html>
